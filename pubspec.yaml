name: cacao_apps
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.10.7

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  
  # Camera and Permissions
  camera: ^0.11.0      #
  permission_handler: ^11.0.0

  flutter_map: ^6.0.0 
  latlong2: ^0.9.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0  #

flutter:
  uses-material-design: true

  # Ensure the folder assets/images/ exists in your project
  assets:
    - assets/images/